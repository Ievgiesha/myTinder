package com.example.my.tinder.Service;

import com.example.my.tinder.model.Person;
import com.example.my.tinder.model.PersonRepository;

import java.util.List;
import java.util.stream.Collectors;

public class PersonSortsClass {
    PersonRepository repository;

    public static void main(String[] args) {


//repository.findById(20,30);

    }
    /*List<Person> sortByAge(int from, int to){
       List<Person> personList = repository.findAll();
    return  personList.stream().filter((person -> person.getAge()>from & person.getAge()< to)).collect(Collectors.toList());*/
    //}
}
